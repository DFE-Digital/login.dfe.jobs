Dear <%=firstName%> <%=lastName%>

A new service has been added to your DfE Sign-in account.

Your account details

Organisation: <%=orgName%>
<% if (permission.id !== undefined) { %>
Permission level: <%=permission.name%>
<% } %>
Service: <%=serviceName%>
Sub-service: <% if (requestedSubServices.length > 0) { %>
                <% for (let x = 0; x < requestedSubServices.length; x++) { %>
                    <%= requestedSubServices[x] %> <br>
                <% } %>
            <% } else { %>
              No roles selected
            <% } %>
Status: Approved

<%
  if (permission.id === 10000) {
    const permissionCapitalise = permission.name.replace(/^./, x => x.toUpperCase());
    const permissionLowercase =  permission.name.toLowerCase();
%>

You have an <%=permissionLowercase%> account. <%=permissionCapitalise%>s can manage other users at their organisation and add services to their account.

Find out what you can do as a DfE <%=permissionLowercase%> <%=helpApproverUrl%>
<% } %>

To access this service, sign in to your DfE Sign-in account using this link <%=signInUrl%>. Sign in as you normally would and select the new service from the list on your services page.
                
Alternatively, select the button below to sign in to your account.

Go to your DfE Sign-in account <%=signInUrl%>

Kind regards,
The Department for Education Sign-in Team
Need support? Visit DfE Sign-in Help <%=helpUrl%>

This is an automatically generated email, do not reply
Received this email in error? No action is necessary